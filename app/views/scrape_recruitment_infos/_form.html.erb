<%= form_with(model: scrape_recruitment_info, local: true) do |form| %>
  <% if scrape_recruitment_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scrape_recruitment_info.errors.count, "error") %> prohibited this scrape_recruitment_info from being saved:</h2>

      <ul>
      <% scrape_recruitment_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :group %>
    <%= form.text_field :group %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
